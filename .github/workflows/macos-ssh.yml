name: macOS SSH

on:
  workflow_dispatch:

jobs:
  remote:
    runs-on: macos-latest
    timeout-minutes: 360

    steps:
      - name: Enable Remote Login
        run: |
          sudo systemsetup -setremotelogin on
          sudo systemsetup -getremotelogin
          sudo lsof -nP -iTCP:22 -sTCP:LISTEN || true

      - name: Install tools
        run: |
          brew install ngrok/ngrok/ngrok tmux htop
          ngrok version
          tmux -V

      - name: Configure SSH key
        env:
          PUBKEY: ${{ secrets.SSH_PUBLIC_KEY }}
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "$PUBKEY" > ~/.ssh/authorized_keys
          chmod 600 ~/.ssh/authorized_keys

          echo "=== authorized_keys ==="
          cat ~/.ssh/authorized_keys

          echo "=== fingerprint ==="
          ssh-keygen -lf ~/.ssh/authorized_keys || true

      - name: Configure sshd
        run: |
          sudo sed -i '' 's/^#\?PubkeyAuthentication.*/PubkeyAuthentication yes/' /etc/ssh/sshd_config
          sudo sed -i '' 's/^#\?PasswordAuthentication.*/PasswordAuthentication no/' /etc/ssh/sshd_config

          if ! grep -q '^AuthorizedKeysFile /Users/runner/.ssh/authorized_keys$' /etc/ssh/sshd_config; then
            echo 'AuthorizedKeysFile /Users/runner/.ssh/authorized_keys' | sudo tee -a /etc/ssh/sshd_config
          fi

          sudo launchctl kickstart -k system/com.openssh.sshd || true
          sleep 3

          echo "=== ssh listening ==="
          sudo lsof -nP -iTCP:22 -sTCP:LISTEN || true

      - name: Prepare shell environment
        run: |
          cat >> ~/.zshrc <<'EOF'
          alias ll='ls -la'
          alias gs='git status'
          alias ..='cd ..'
          alias ...='cd ../..'
          EOF

          cat >> ~/.bashrc <<'EOF'
          alias ll='ls -la'
          alias gs='git status'
          alias ..='cd ..'
          alias ...='cd ../..'
          EOF

          mkdir -p ~/work
          echo "Runner ready at $(date)" > ~/work/README.txt

      - name: Start tmux session
        run: |
          tmux new-session -d -s remote
          tmux send-keys -t remote 'echo "tmux session: remote"' C-m
          tmux send-keys -t remote 'echo "Use: tmux attach -t remote"' C-m
          tmux ls

      - name: Write ngrok config
        env:
          NGROK_TOKEN: ${{ secrets.NGROK_TOKEN }}
        run: |
          mkdir -p "$HOME/Library/Application Support/ngrok"

          cat > "$HOME/Library/Application Support/ngrok/ngrok.yml" <<EOF
          version: 3
          agent:
            authtoken: $NGROK_TOKEN
            web_addr: 127.0.0.1:4040
          tunnels:
            ssh:
              proto: tcp
              addr: 22
          EOF

          echo "=== ngrok config ==="
          cat "$HOME/Library/Application Support/ngrok/ngrok.yml"

      - name: Start ngrok
        run: |
          nohup ngrok start --all --log stdout --log-format json > ngrok.log 2>&1 &
          sleep 15

          echo "=== ngrok process ==="
          ps aux | grep '[n]grok' || true

          echo "=== ngrok log ==="
          cat ngrok.log || true

      - name: Get tunnel info
        run: |
          curl -s http://127.0.0.1:4040/api/tunnels > tunnels.json || true

          echo "=== tunnels.json ==="
          cat tunnels.json || true

          if [ ! -s tunnels.json ]; then
            echo "tunnels.json vacío"
            exit 1
          fi

      - name: Print SSH command
        run: |
          python3 <<'PY'
          import json
          import sys
          
          with open("tunnels.json", "r", encoding="utf-8") as f:
              data = json.load(f)
          
          for t in data.get("tunnels", []):
              if t.get("name") == "ssh":
                  url = t.get("public_url", "")
                  if url.startswith("tcp://"):
                      hp = url.replace("tcp://", "")
                      host, port = hp.split(":")
                      print("")
                      print("SSH CONNECT:")
                      print(f"ssh -o IdentitiesOnly=yes -i C:\\Users\\Jorge\\.ssh\\id_ed25519 runner@{host} -p {port}")
                      print("")
                      print("Useful after login:")
                      print("  tmux attach -t remote")
                      print("  cd ~/work")
                      print("")
                      sys.exit(0)
          
          print("No SSH tunnel found")
          sys.exit(1)
          PY
          
                - name: Keep runner alive
                  run: |
                    while true; do
                      echo alive
                      date
                      sleep 60
                    done
