name: macOS SSH

on:
  workflow_dispatch:

jobs:
  remote:
    runs-on: macos-latest
    timeout-minutes: 360

    steps:
      - name: Enable Remote Login
        run: |
          sudo systemsetup -setremotelogin on
          sudo systemsetup -getremotelogin
          sudo lsof -nP -iTCP:22 -sTCP:LISTEN || true

      - name: Install ngrok
        run: |
          brew install ngrok/ngrok/ngrok
          ngrok version

      - name: Configure SSH authorized_keys
        env:
          PUBKEY: ${{ secrets.SSH_PUBLIC_KEY }}
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "$PUBKEY" > ~/.ssh/authorized_keys
          chmod 600 ~/.ssh/authorized_keys

          echo "=== authorized_keys ==="
          cat ~/.ssh/authorized_keys

          echo "=== fingerprint ==="
          ssh-keygen -lf ~/.ssh/authorized_keys || true

      - name: Configure sshd
        run: |
          sudo sed -i '' 's/#PubkeyAuthentication.*/PubkeyAuthentication yes/' /etc/ssh/sshd_config
          sudo sed -i '' 's/#PasswordAuthentication.*/PasswordAuthentication no/' /etc/ssh/sshd_config

          echo "AuthorizedKeysFile /Users/runner/.ssh/authorized_keys" | sudo tee -a /etc/ssh/sshd_config

          sudo launchctl kickstart -k system/com.openssh.sshd || true
          sleep 3

          echo "=== ssh listen ==="
          sudo lsof -nP -iTCP:22 -sTCP:LISTEN || true

      - name: Configure ngrok
        env:
          NGROK_TOKEN: ${{ secrets.NGROK_TOKEN }}
        run: |
          ngrok config add-authtoken "$NGROK_TOKEN"
          nohup ngrok tcp 22 --log stdout > ngrok.log 2>&1 &
          sleep 10

          echo "=== ngrok process ==="
          ps aux | grep '[n]grok' || true

          echo "=== ngrok log ==="
          cat ngrok.log || true

      - name: Get tunnel info
        run: |
          curl -s http://127.0.0.1:4040/api/tunnels > tunnels.json || true

          echo "=== tunnels.json ==="
          cat tunnels.json || true

      - name: Print SSH command
        run: |
          python3 <<'PY'
          import json
          import os

          if not os.path.exists("tunnels.json"):
            print("tunnels.json no existe")
            exit(1)

          data = json.load(open("tunnels.json"))

          for t in data.get("tunnels", []):
            url = t.get("public_url", "")
            if url.startswith("tcp://"):
              hp = url.replace("tcp://", "")
              host, port = hp.split(":")
              print("")
              print("SSH CONNECT:")
              print(f"ssh -vvv -o IdentitiesOnly=yes -i C:\\Users\\Jorge\\.ssh\\id_ed25519 runner@{host} -p {port}")
              print("")
          break
          PY

      - name: Keep runner alive
        run: |
          while true; do
            echo alive
            date
            sleep 60
          done
